spring:
  cloud:
    nacos:
      server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8888
  application:
    name: sentinel-provider

server:
  port: 9999


# 暴露actuator端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
