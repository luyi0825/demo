spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      server-addr: localhost:8850
      config:
        file-extension: yml
      discovery:
        weight: 2
        server-addr: localhost:8850
server:
  port: 0
