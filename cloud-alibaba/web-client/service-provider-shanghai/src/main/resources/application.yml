spring:
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        ## 集群名称:上海
        cluster-name: shanghai
        weight: 100
  application:
    name: webclient-provider
server:
  port: 0
